import React from 'react';

// This component has been replaced by the manual BoardEditor and Dashboard.
// Kept as a placeholder to avoid breaking old imports if any exist, though App.tsx no longer uses it.
export const GameBuilder = () => <div />;
